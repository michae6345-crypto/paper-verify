DATABASE_URL=postgresql://postgres:dev@localhost:5432/verify
STORAGE_BACKEND=local
STORAGE_PATH=./storage
QUEUE_BACKEND=inline

OPENROUTER_API_KEY=
LLM_MODEL=openrouter/free
LLM_FALLBACK_MODELS=
LLM_ENABLED=true
LLM_CACHE_DIR=./.llmcache
LLM_RPM=18
LLM_CONCURRENCY=4

EMBEDDING_BACKEND=local
EMBEDDING_DIM=384

GITHUB_TOKEN=
CONTACT_EMAIL=michae6345@gmail.com

HTTP_BACKEND=live
HTTP_CACHE_DIR=./.httpcache
HTTP_CACHE_TTL_SECONDS=86400
HTTP_CONCURRENCY=8
HTTP_MIN_INTERVAL=1.0
HTTP_TIMEOUT=30

# Absolute, or relative to the repo root -- never to the current directory.
ARXIV_CACHE_DIR=
